# Project overview

WeatherNest is a personal weather dashboard that combines:
- **External forecast data** from Open-Meteo (free, no API key required)
- **DIY sensor readings** pushed from home devices (ESP32 + DHT22 and similar)

The goal is a self-hosted, containerised app that can be run locally (Docker lab) and later
deployed to AWS.
